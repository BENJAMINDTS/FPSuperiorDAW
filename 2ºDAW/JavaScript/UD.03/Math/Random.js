//We use Math.random() to generate a random number between 0 (inclusive) and 10 (exclusive)
let randomNum = Math.random() * 10
randomNum = Math.floor(randomNum) //We use Math.floor() to round down to the nearest integer
console.log(randomNum) //This will print a random integer between 0 and 9