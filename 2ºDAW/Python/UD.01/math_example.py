"""
# Módulo Math

Ejemplo de uso del módulo estándar `math`.
Calcula la raíz cuadrada de un número usando `math.sqrt()`.
"""

from math import sqrt

number = 25
result = sqrt(number)  # Calcula la raíz cuadrada del número
print("La raiz cuadrada de", number, "es", result)
